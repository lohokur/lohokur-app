'use client';

// Pay-first: the free trial has been retired, so there's no trial countdown to show.
// Kept as an inert component so existing imports/renders stay valid.
export default function TrialBadge() {
  return null;
}
